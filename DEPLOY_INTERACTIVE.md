# 🚀 FiatLux Interactive Deploy Tool (Windows)

Інтерактивний інструмент для деплою FiatLux на сервер з Docker через одне меню.

## 📋 Вимоги

- **Windows 10/11** з PowerShell 5.1+
- **SSH клієнт** (встановлений за замовчуванням в Windows 10+)
- **SSH ключи** налаштовані для з'єднання з сервером
- **Git** (опційно, для оновлень)

### Встановлення на Windows

```powershell
# Дозволяємо виконання скриптів
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# Перевіряємо SSH
ssh -V
```

## 🔑 Підготовка SSH

### Генерування ключів

```bash
# На Windows (PowerShell або Git Bash)
ssh-keygen -t rsa -b 4096 -C "your_email@example.com"

# Ключ буде збережений в:
# ~/.ssh/id_rsa (приватний)
# ~/.ssh/id_rsa.pub (публічний)
```

### Копіювання ключа на сервер

```bash
# З Windows PowerShell
type $env:USERPROFILE\.ssh\id_rsa.pub | ssh ubuntu@server-ip "cat >> ~/.ssh/authorized_keys"

# Або через SSH копіювання
ssh-copy-id -i $env:USERPROFILE\.ssh\id_rsa.pub ubuntu@server-ip
```

## 🚀 Використання

### 1. Запуск скрипту

```powershell
# Дозволяємо виконання скриптів (один раз)
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# Запускаємо інтерактивний деплой
.\deploy-interactive.ps1
```

### 2. Головне меню

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  🚀 FiatLux Docker Deploy Tool
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. 🔧 Нова конфігурація
2. 📝 Редагувати конфігурацію
3. ✓ Перевірити конфігурацію
4. 🚀 Запустити деплой
5. 📊 Перевірити статус деплою
6. 🔄 Оновити код (git pull)
7. 🛑 Зупинити сервіс
8. 📋 Переглянути логи
9. ❌ Видалити контейнер
0. 🚪 Вихід
```

## 📝 Опціоні меню

### 1. Нова конфігурація / Редагувати конфігурацію

Налаштування основних параметрів:

- **IP адреса сервера** - IP або домен вашого сервера
- **Користувач SSH** - зазвичай `ubuntu`
- **SSH ключ** - шлях до приватного ключа
- **Порт** - порт на якому запустити сервіс (за замовчуванням 3000)
- **URL репозиторію** - GitHub URL вашого проекту
- **API_ID Telegram** - з https://my.telegram.org
- **API_HASH Telegram** - з https://my.telegram.org
- **Session String** - буде згенеровано автоматично
- **Назва каналу** - Telegram канал для моніторингу

Конфігурація автоматично зберігається в `.fiatlux-config.json`

### 2. Перевірити конфігурацію

Перевіряє:
- ✓ Усі необхідні поля заповнені
- ✓ SSH ключ існує
- ✓ З'єднання з сервером через SSH
- ✓ Docker встановлений на сервері
- ✓ Docker Compose встановлений на сервері

### 3. Запустити деплой

Автоматично виконує:

1. Підготовка директорії на сервері
2. Клонування або оновлення репозиторію
3. Налаштування `.env` файлу з вашими credentials
4. Зупинення старих контейнерів
5. Побудова Docker образу
6. Запуск контейнера
7. Перевірка статусу сервісу
8. Перевірка доступності API

### 4. Перевірити статус деплою

Показує:
- Статус запущених контейнерів
- Порти, виділені контейнеру
- Останні 10 рядків логів

### 5. Оновити код (git pull)

- Витягує найновіший код з репозиторію
- Перебудовує Docker образ
- Перезапускає сервіс

### 6. Зупинити сервіс

Зупиняє контейнер, але не видаляє дані.

### 7. Переглянути логи

Показує логи в реальному часі. Натисніть **Ctrl+C** щоб вийти.

### 8. Видалити контейнер

⚠️ **Обережно!** Видаляє контейнер та усі дані у базі.

## 🔄 Приклад деплою

```powershell
# Запускаємо скрипт
.\deploy-interactive.ps1

# Вибираємо опцію 1 (Нова конфігурація) або 2 (Редагувати)
# Заповнюємо дані:
# - Сервер: 192.168.1.100
# - Користувач: ubuntu
# - SSH ключ: C:\Users\YourName\.ssh\id_rsa
# - Репозиторій: https://github.com/your-repo/FiatLux.git
# - API_ID: 34405296
# - API_HASH: b1b057965806f8ecadbf5fab65272b3e
# - Канал: pat_cherkasyoblenergo

# Вибираємо опцію 3 (Перевірити конфігурацію)
# Перевіряємо що все налаштовано правильно

# Вибираємо опцію 4 (Запустити деплой)
# Підтверджуємо та чекаємо завершення деплою

# Вибираємо опцію 5 (Перевірити статус)
# Переконуємось що сервіс запущений

# Готово! Сервіс доступний на http://192.168.1.100:3000
```

## 🔐 Безпека

### Рекомендації

1. **SSH ключі:**
   - Захищайте приватний ключ від доступу
   - Не ділітеся приватним ключем
   - Встановіть правильні дозволи: `chmod 600 ~/.ssh/id_rsa`

2. **Firewall:**
   ```bash
   sudo ufw allow ssh
   sudo ufw allow 3000/tcp
   sudo ufw enable
   ```

3. **Реверс-прокси (Nginx):**
   ```bash
   sudo apt install nginx
   ```
   
   `/etc/nginx/sites-available/fiatlux`:
   ```nginx
   server {
       listen 80;
       server_name your-domain.com;
       
       location / {
           proxy_pass http://localhost:3000;
           proxy_set_header Host $host;
           proxy_set_header X-Real-IP $remote_addr;
           proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
       }
   }
   ```

4. **SSL сертифікат (Let's Encrypt):**
   ```bash
   sudo apt install certbot python3-certbot-nginx
   sudo certbot --nginx -d your-domain.com
   ```

## 🐛 Розв'язання проблем

### Проблема: "Не можу підключитися до сервера"

**Рішення:**
```powershell
# Перевірте SSH ключ
Test-Path $env:USERPROFILE\.ssh\id_rsa

# Спробуйте вручну підключитися
ssh -i $env:USERPROFILE\.ssh\id_rsa ubuntu@your-server-ip

# Перевірте firewall правила
# На сервері: sudo ufw allow ssh
```

### Проблема: "Docker не встановлений"

**Рішення:**
```bash
# На сервері (SSH)
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
sudo usermod -aG docker $USER
newgrp docker
```

### Проблема: "Контейнер не запускається"

**Рішення:**
```powershell
# У меню виберіть "8. Переглянути логи"
# або вручну:
ssh -i $key ubuntu@server "cd /opt/fiatlux && docker-compose logs --tail=50"
```

### Проблема: "Порт уже зайнятий"

**Рішення:**
1. Виберіть "2. Редагувати конфігурацію"
2. Виберіть "4. Порт" и введіть новий порт (наприклад, 3001)
3. Запустіть деплой заново

### Проблема: "Session String помилка"

**Рішення:**
```powershell
# Видаліть старий сеанс
ssh -i $key ubuntu@server "rm -rf /opt/fiatlux/data/session.session"

# Перезапустіть сервіс
ssh -i $key ubuntu@server "cd /opt/fiatlux && docker-compose restart"
```

## 💾 Конфігураційний файл

Конфігурація автоматично зберігається у `.fiatlux-config.json` в тій же папці що й скрипт.

Приклад:
```json
{
    "Server": "192.168.1.100",
    "Port": 3000,
    "User": "ubuntu",
    "PrivateKeyPath": "C:\\Users\\YourName\\.ssh\\id_rsa",
    "AppDir": "/opt/fiatlux",
    "RepoUrl": "https://github.com/your-repo/FiatLux.git",
    "ChannelUsername": "pat_cherkasyoblenergo"
}
```

## 📞 Корисні команди

Якщо вам потрібно щось зробити вручну:

```bash
# Підключитися до сервера
ssh -i "C:\Users\YourName\.ssh\id_rsa" ubuntu@server-ip

# На сервері:

# Переглянути логи
cd /opt/fiatlux && docker-compose logs -f

# Перезапустити сервіс
cd /opt/fiatlux && docker-compose restart

# Зупинити сервіс
cd /opt/fiatlux && docker-compose down

# Переглянути процеси
docker ps | grep fiatlux

# Видалити контейнер
cd /opt/fiatlux && docker-compose down -v
```

## 🎯 Быстрий старт

```powershell
# 1. Дозволяємо виконання скриптів
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# 2. Запускаємо инструмент
.\deploy-interactive.ps1

# 3. Вибираємо опцію 1 (нова конфігурація)
# 4. Вибираємо опцію 3 (перевірка)
# 5. Вибираємо опцію 4 (деплой)
# 6. Вибираємо опцію 5 (статус)

# Готово! ✨
```

## 📖 Допомога

Для отримання більше інформації про команди меню просто запустіть скрипт та слідкуйте за підказками.
