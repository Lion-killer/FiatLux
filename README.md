# FiatLux 💡

Сервіс для моніторингу Telegram каналу Черкасиобленерго та отримання графіків відключень електроенергії у форматі JSON через REST API.

## Особливості

- 🔄 **Real-time моніторинг** - автоматичне відстеження нових повідомлень у Telegram каналі
- 📊 **Розумний парсинг** - автоматичне розпізнавання графіків відключень з повідомлень
- 🎯 **REST API** - зручний HTTP інтерфейс для отримання даних
- 💾 **JSON сховище** - збереження даних у локальних JSON файлах
- 📝 **Історія графіків** - збереження попередніх графіків з можливістю перегляду
- 🔍 **Розділення графіків** - автоматичне визначення поточного та майбутнього графіків

## Передумови

- Node.js >= 18.0.0
- Telegram акаунт
- API credentials від Telegram (API ID та API Hash)

## Отримання Telegram API Credentials

1. Перейдіть на https://my.telegram.org
2. Увійдіть за допомогою номера телефону
3. Перейдіть у розділ "API development tools"
4. Створіть нову аплікацію
5. Отримайте `api_id` та `api_hash`

## Встановлення

1. Клонуйте репозиторій:
```bash
git clone <repository-url>
cd FiatLux
```

2. Встановіть залежності:
```bash
npm install
```

3. Створіть файл `.env` на основі `.env.example`:
```bash
cp .env.example .env
```

4. Відредагуйте `.env` файл і додайте ваші Telegram credentials:
```env
API_ID=your_api_id
API_HASH=your_api_hash
CHANNEL_USERNAME=cherkasyoblenergo
PORT=3000
```

> **Примітка**: `SESSION_STRING` буде згенеровано автоматично при першому запуску. Скопіюйте його у `.env` файл для наступних запусків.

## Компіляція

Скомпілюйте TypeScript код:
```bash
npm run build
```

## Запуск

### Production режим:
```bash
npm start
```

### Development режим (з автоматичною перекомпіляцією):
```bash
npm run dev
```

При першому запуску вам потрібно буде авторизуватися у Telegram:
1. Введіть номер телефону (у форматі +380...)
2. Введіть код підтвердження з Telegram
3. Якщо потрібно, введіть пароль двофакторної автентифікації

Після успішної авторизації з'явиться `SESSION_STRING` - збережіть його у `.env` файл.

## API Endpoints

### Отримати інформацію про API
```bash
GET /
```

**Відповідь:**
```json
{
  "name": "FiatLux - Telegram Channel Monitor",
  "version": "1.0.0",
  "description": "Monitoring Cherkasyoblenergo power outage schedules",
  "endpoints": {...}
}
```

### Перевірка стану сервісу
```bash
GET /api/health
```

**Відповідь:**
```json
{
  "success": true,
  "data": {
    "status": "ok",
    "uptime": 3600,
    "telegramConnected": true,
    "lastMessageCheck": "2026-02-14T10:30:00.000Z",
    "schedulesCount": 5
  },
  "timestamp": "2026-02-14T10:35:00.000Z"
}
```

### Отримати поточний графік
```bash
GET /api/schedule/current
```

**Відповідь:**
```json
{
  "success": true,
  "data": {
    "id": "12345-2026-02-14",
    "type": "current",
    "date": "2026-02-14",
    "queues": [
      {
        "queueNumber": 1,
        "timeSlots": [
          {"start": "08:00", "end": "12:00"},
          {"start": "16:00", "end": "20:00"}
        ],
        "description": "Черга 1"
      }
    ],
    "rawText": "Графік відключень на 14.02.2026...",
    "publishedAt": "2026-02-14T08:00:00.000Z",
    "messageId": 12345,
    "channelId": "1234567890",
    "archived": false
  },
  "timestamp": "2026-02-14T10:35:00.000Z"
}
```

### Отримати майбутній графік
```bash
GET /api/schedule/future
```

**Відповідь:** Аналогічна структура або `null` якщо майбутній графік відсутній.

### Отримати всі графіки (поточний + майбутній)
```bash
GET /api/schedule/all
```

**Відповідь:**
```json
{
  "success": true,
  "data": {
    "current": {...},
    "future": {...}
  },
  "timestamp": "2026-02-14T10:35:00.000Z"
}
```

### Отримати історію графіків
```bash
GET /api/schedule/history?limit=10
```

**Параметри:**
- `limit` (опціонально) - кількість записів (за замовчуванням: 10)

### Примусове оновлення графіків
```bash
POST /api/refresh
```

Примусово завантажує останні 50 повідомлень з каналу та парсить їх.

## Структура проєкту

```
FiatLux/
├── src/
│   ├── api/
│   │   └── server.ts           # Express REST API сервер
│   ├── config/
│   │   └── index.ts            # Конфігурація з .env
│   ├── parsers/
│   │   └── scheduleParser.ts   # Парсер графіків відключень
│   ├── storage/
│   │   └── dataManager.ts      # Менеджер JSON сховища
│   ├── telegram/
│   │   └── client.ts           # Telegram клієнт
│   ├── types/
│   │   └── index.ts            # TypeScript типи
│   ├── utils/
│   │   └── logger.ts           # Логування
│   └── index.ts                # Точка входу
├── data/
│   └── schedules.json          # Збережені графіки
├── dist/                       # Скомпільований JavaScript
├── .env                        # Змінні середовища (створити вручну)
├── .env.example                # Приклад змінних середовища
├── .gitignore
├── package.json
├── tsconfig.json
└── README.md
```

## Приклади використання

### curl
```bash
# Отримати поточний графік
curl http://localhost:3000/api/schedule/current

# Отримати всі графіки
curl http://localhost:3000/api/schedule/all

# Примусове оновлення
curl -X POST http://localhost:3000/api/refresh
```

### JavaScript (fetch)
```javascript
// Отримати поточний графік
fetch('http://localhost:3000/api/schedule/current')
  .then(res => res.json())
  .then(data => {
    if (data.success && data.data) {
      console.log('Поточний графік:', data.data);
      console.log('Черги:', data.data.queues);
    }
  });
```

### Python (requests)
```python
import requests

response = requests.get('http://localhost:3000/api/schedule/all')
data = response.json()

if data['success']:
    current = data['data']['current']
    future = data['data']['future']
    
    if current:
        print(f"Поточний графік на {current['date']}")
        for queue in current['queues']:
            print(f"Черга {queue['queueNumber']}: {queue['timeSlots']}")
```

## Налаштування

### Змінні середовища (.env)

- `API_ID` - Telegram API ID (обов'язково)
- `API_HASH` - Telegram API Hash (обов'язково)
- `SESSION_STRING` - Сесія Telegram (генерується автоматично)
- `CHANNEL_USERNAME` - Username каналу для моніторингу (за замовчуванням: cherkasyoblenergo)
- `PORT` - Порт API сервера (за замовчуванням: 3000)
- `HOST` - Host API сервера (за замовчуванням: 0.0.0.0)
- `DATA_DIR` - Папка для збереження даних (за замовчуванням: ./data)
- `LOG_LEVEL` - Рівень логування: debug, info, warn, error (за замовчуванням: info)

## Troubleshooting

### Помилка "Missing required environment variable"
Переконайтеся, що всі обов'язкові змінні (`API_ID`, `API_HASH`) вказані у `.env` файлі.

### Помилка авторизації Telegram
1. Перевірте правильність API credentials
2. Переконайтеся, що вказали правильний номер телефону (з кодом країни)
3. Видаліть `SESSION_STRING` з `.env` та спробуйте авторизуватися знову

### Сервіс не знаходить графіки
1. Перевірте, що канал `cherkasyoblenergo` доступний
2. Переконайтеся, що у каналі є повідомлення з ключовими словами про графіки
3. Спробуйте викликати `/api/refresh` для примусового оновлення

### Порт зайнятий
Змініть `PORT` у `.env` файлі на інший (наприклад, 3001, 8080).

## Ліцензія

MIT

## Автор

FiatLux - Telegram Monitor Service

---

**Примітка**: Цей сервіс призначений для особистого використання. Переконайтеся, що ви дотримуєтесь умов використання Telegram API.
