# FiatLux 💡

Сервіс для моніторингу Telegram каналу Черкасиобленерго, який автоматично збирає інформацію про графіки відключень електроенергії і відображає їх у зручному web інтерфейсі.

## Як це працює

1. 🔍 **Моніторинг Telegram** - сервіс підключається до Telegram каналу @pat_cherkasyoblenergo
2. 📥 **Збір даних** - автоматично зчитує нові повідомлення про графіки відключень
3. 🧠 **Парсинг інформації** - розпізнає та структурує дані про відключення по чергах та годинах
4. 🌐 **Відображення** - показує інформацію у зручному web інтерфейсі з 36-годинним таймлайном

## Скриншот

![FiatLux Web Interface](docs/images/screenshot.png)

*Web інтерфейс FiatLux з графіками відключень електроенергії*

## ✨ Особливості

### Web Інтерфейс
- 🌐 **Зручний інтерфейс** - перегляд графіків у браузері без додаткових програм
- 📈 **36-годинний таймлайн** - 12 годин назад + поточний момент + 24 години вперед
- 🎨 **Фільтрація по чергах** - виберіть вашу чергу і дивіться тільки актуальну інформацію
- 🔄 **Автоматичне оновлення** - дані оновлюються кожні 30 секунд
- 💾 **Запам'ятовування вибору** - вибрана черга зберігається в браузері
- 🎯 **Кольорове кодування** - червоний (немає світла), зелений (є світло), сірий (немає даних)
- ⏰ **Маркер поточного часу** - фіолетова лінія показує "зараз"

### Технічні можливості
- 🔄 **Real-time моніторинг** - автоматичне відстеження нових повідомлень у Telegram
- 📊 **Розумний парсинг** - автоматичне розпізнавання графіків з української мови
- 🎯 **REST API** - програмний доступ до даних
- 💾 **Оперативне сховище** - збереження даних в пам'яті
- ⚡ **Швидкодія** - мінімальне споживання ресурсів (~50-100MB RAM)

---

## 🚀 Швидкий Старт

### Варіант 1: Docker з веб-налаштуванням (найпростіший)

```bash
# 1. Запустіть контейнер
docker-compose up -d

# 2. Відкрийте браузер
# http://localhost:8080

# 3. Пройдіть веб-налаштування (3 кроки):
#    - Введіть API_ID та API_HASH з https://my.telegram.org
#    - Введіть номер телефону
#    - Підтвердіть код з Telegram

# 4. Готово! Графіки автоматично завантажаться
```

### Варіант 2: Docker з ручним налаштуванням

```bash
# 1. Створіть .env файл
cp .env.example .env

# 2. Отримайте credentials на https://my.telegram.org
# Додайте у .env:
#    API_ID=your_api_id
#    API_HASH=your_api_hash

# 3. Запустіть
docker-compose up -d

# 4. Відкрийте браузер: http://localhost:8080
```

### Варіант 3: Локальна розробка (npm)

```bash
npm install
npm run build
npm start

# При першому запуску введіть:
# - Номер телефону (+380...)
# - Код з Telegram
# - Скопіюйте SESSION_STRING у .env
```

**🎉 Готово!** Відкрийте http://localhost:8080 і бачите графіки відключень.

---

## 📖 Документація

- **[docs/QUICKSTART.md](docs/QUICKSTART.md)** - Детальна інструкція локального запуску
- **[docs/WEB_SETUP.md](docs/WEB_SETUP.md)** - Повний гайд веб-налаштування Telegram
- **[docs/DEPLOY.md](docs/DEPLOY.md)** - Деплой на production сервер (Windows → Linux)
- **[docs/README.md](docs/README.md)** - Індекс усієї документації

---

## 🔌 REST API (коротко)

### Основні endpoint'и

```bash
# Стан сервісу
GET /api/health

# Поточний графік
GET /api/schedule/current

# Майбутній графік
GET /api/schedule/future

# Всі графіки
GET /api/schedule/all

# Примусове оновлення
POST /api/refresh
```

### Приклад відповіді

```json
{
  "success": true,
  "data": {
    "id": "12345-2026-02-15",
    "date": "2026-02-15",
    "queues": [
      {
        "queueNumber": "1.1",
        "timeSlots": [
          {"start": "08:00", "end": "12:00"},
          {"start": "16:00", "end": "20:00"}
        ]
      }
    ]
  }
}
```

---

## 🐳 Docker Deployment

Найпростіший спосіб запустити FiatLux - використати Docker.

### Міграція даних не потрібна

FiatLux зберігає дані в оперативній пам'яті (RAM) і завжди завантажує актуальні графіки з Telegram при запуску. Не потрібна база даних або persistent storage.

### Автоматичний деплой на віддалений сервер (Windows)

Для деплою на production сервер використовуйте інтерактивний скрипт:

```powershell
.\deploy.ps1
```

Скрипт автоматично:
- ✅ Налаштує SSH ключі
- ✅ Перевірить наявність Docker на сервері
- ✅ Склонує репозиторій
- ✅ Створить та запустить контейнер
- ✅ Перевірить статус деплою

**Детальна інструкція:** [docs/DEPLOY.md](docs/DEPLOY.md)

### Ручний деплой

```bash
# На сервері
git clone <repository-url>
cd FiatLux

# Створіть .env
cp .env.example .env
nano .env  # Додайте API_ID, API_HASH

# Запустіть
docker-compose up -d

# Перегляд логів
docker-compose logs -f
```

---

## ⚙️ Налаштування

### Змінні середовища (.env)

| Змінна | Опис | Обов'язково |
|--------|------|-------------|
| `API_ID` | Telegram API ID з https://my.telegram.org | ✅ Так |
| `API_HASH` | Telegram API Hash | ✅ Так |
| `SESSION_STRING` | Telegram сесія (генерується автоматично) | Ні |
| `CHANNEL_USERNAME` | Канал для моніторингу | Ні (за замовчуванням: `pat_cherkasyoblenergo`) |
| `PORT` | Порт API сервера | Ні (за замовчуванням: `8080`) |
| `HOST` | Host API сервера | Ні (за замовчуванням: `0.0.0.0`) |
| `LOG_LEVEL` | Рівень логування (debug/info/warn/error) | Ні (за замовчуванням: `info`) |

### Отримання Telegram API Credentials

1. Відкрийте https://my.telegram.org
2. Увійдіть з номером телефону
3. Перейдіть в **"API development tools"**
4. Створіть application
5. Скопіюйте `api_id` та `api_hash`

**Детально:** [docs/QUICKSTART.md](docs/QUICKSTART.md#3-отримання-telegram-api-credentials)

---

## 📁 Структура проєкту

```
FiatLux/
├── src/
│   ├── api/              # Express API сервер
│   ├── telegram/         # Telegram клієнт + авторизація
│   ├── parsers/          # Парсер графіків відключень
│   ├── storage/          # In-memory сховище
│   └── index.ts          # Entry point
├── public/
│   ├── index.html        # Web інтерфейс (таймлайн)
│   └── setup.html        # Telegram setup wizard
├── docs/                 # Документація
├── docker-compose.yml
├── Dockerfile
└── deploy.ps1            # Windows → Linux deployment
```

---

## 🐛 Troubleshooting

### Помилка "Missing required environment variable"
→ Перевірте `.env`: `API_ID` та `API_HASH` мають бути заповнені

### Помилка авторизації Telegram
→ Видаліть `SESSION_STRING` з `.env` та пройдіть авторизацію знову

### Сервіс не знаходить графіки
→ Викличте `POST /api/refresh` для примусового оновлення

### Порт зайнятий
→ Змініть `PORT` у `.env` на інший (наприклад, 3001)

**Детальні рішення:** [docs/DEPLOY.md](docs/DEPLOY.md#-troubleshooting)

---

## 🤝 Contributing

Pull requests вітаються! Для великих змін спочатку відкрийте issue для обговорення.

## 📄 Ліцензія

MIT

---

**Примітка**: Цей сервіс призначений для особистого використання. Дотримуйтесь умов використання Telegram API.
